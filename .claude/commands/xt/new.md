---
name: "XT: 新建"
description: 启动新 OpenSpec 变更（ xt 封装层）
category: 工作流
tags: [workflow, artifacts]
---

转发到 `opsx:new` 命令。

**输入**：`/xt:new` 之后的参数是变更名称（kebab-case），或用户想要构建内容的描述。

详见：`.claude/commands/opsx/new.md`
