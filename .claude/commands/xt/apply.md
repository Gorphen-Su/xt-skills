---
name: "XT: 应用"
description: 实现 OpenSpec 变更中的任务（ xt 封装层）
category: 工作流
tags: [workflow, tasks]
---

转发到 `opsx:apply` 命令。

**输入**：可选择指定变更名称（例如，`/xt:apply add-auth`）。

详见：`.claude/commands/opsx/apply.md`
