## 新增需求

### 需求:核心模块测试覆盖
系统必须为核心模块提供单元测试，代码覆盖率应达到 80% 以上。

#### 场景:核心模块测试执行
- **当** 开发者运行 `npm test:core`
- **那么** 所有核心模块的测试被运行并显示覆盖率报告

### 需求:测试数据模拟工具
系统必须提供模拟常见数据类型的工具函数。

#### 场景:创建模拟订单数据
- **当** 测试需要订单数据
- **那么** 可以调用 `createOrderFixture()` 获取包含默认值的订单对象

## 修改需求

## 移除需求
