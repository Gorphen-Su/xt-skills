## 新增需求

### 需求:测试fixture工具函数
系统必须提供 fixture 工具函数，用于生成测试数据。

#### 场景:创建用户fixture
- **当** 测试需要用户数据
- **那么** 调用 `createUserFixture()` 返回带有默认值的用户对象

#### 场景:创建订单fixture
- **当** 测试需要订单数据
- **那么** 调用 `createOrderFixture()` 返回带有默认值的订单对象

## 修改需求

## 移除需求
